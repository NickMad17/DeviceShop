export enum Paths {
    ERROR = '*',
    HOME = "/",
    START = '/start',
    ACCOUNT = '/account',
    FAVORITES = '/favorites',
    PRODUCTS = '/products',
    CART = '/cart',
    PRODUCT = 'product'
}
